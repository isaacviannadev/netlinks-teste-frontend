import styled from 'styled-components';

const PhotobarWrapper = styled.section`
  display: flex;
  width: 100%;
  height: 100%;
  min-height: 300px;
  background: #3039c3 url('/photobar.png') no-repeat right top / cover;
`;

export default PhotobarWrapper;
